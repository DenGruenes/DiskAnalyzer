╔═══════════════════════════════════════════════════════════════════════════════╗
║                   GITHUB UPLOAD ANLEITUNG - DiskAnalyzer                     ║
╚═══════════════════════════════════════════════════════════════════════════════╝

Das Projekt ist vollständig vorbereitet und in Git initialisiert!
Hier ist eine Schritt-für-Schritt Anleitung zum Hochladen auf GitHub:

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 1: GitHub Repository erstellen

1. Gehe zu https://github.com/new
2. Gib folgende Informationen ein:

   Repository name:      DiskAnalyzer
   Description:          TreeSize Alternative - Hochperformante Festplattenanalyse für Windows
   Visibility:           Public (oder Private wenn gewünscht)
   Initialize repository: NICHT anklicken (wir haben schon Commits!)
   License:              MIT (optional, wir haben schon LICENSE-Datei)
   .gitignore:           Pascal/Delphi (optional, wir haben schon .gitignore)

3. Klicke "Create repository"

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 2: Git Remote konfigurieren

Gib folgende Befehle in deinem Terminal aus:

```bash
cd /path/to/DiskAnalyzer
git remote add origin https://github.com/DEIN_USERNAME/DiskAnalyzer.git
```

Ersetze DEIN_USERNAME mit deinem GitHub-Benutzernamen!

Beispiel:
```bash
git remote add origin https://github.com/jsmith/DiskAnalyzer.git
```

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 3: Überprüfe die Remote-Konfiguration

```bash
git remote -v
```

Ausgabe sollte so aussehen:
```
origin  https://github.com/dein-username/DiskAnalyzer.git (fetch)
origin  https://github.com/dein-username/DiskAnalyzer.git (push)
```

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 4: Initial Branch benennen (optional)

Wenn du "main" statt "master" verwenden möchtest:

```bash
git branch -M main
```

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 5: Code auf GitHub pushen

```bash
# Push Master-Branch mit allen Commits
git push -u origin master

# oder wenn du "main" verwendest:
git push -u origin main

# Push Tags (Versionen)
git push origin --tags
```

Hinweis: Du wirst möglicherweise nach Authentifizierung gefragt.

═══════════════════════════════════════════════════════════════════════════════

## AUTHENTIFIZIERUNG (SSH oder HTTPS)

### Option A: SSH (empfohlen)

1. SSH-Schlüssel generieren (falls noch nicht vorhanden):
   ```bash
   ssh-keygen -t ed25519 -C "deine-email@example.com"
   ```

2. Öffentlichen Schlüssel zu GitHub hinzufügen:
   - GitHub Settings → SSH and GPG keys
   - "New SSH key"
   - Inhalt aus ~/.ssh/id_ed25519.pub einfügen

3. Remote-URL auf SSH umstellen:
   ```bash
   git remote set-url origin git@github.com:dein-username/DiskAnalyzer.git
   ```

### Option B: HTTPS mit Token (einfacher für Anfänger)

1. Erstelle einen GitHub Personal Access Token:
   - GitHub Settings → Developer settings → Personal access tokens
   - "Generate new token"
   - Gib "repo" Permissions
   - Kopiere den Token

2. Verwende diesen Command:
   ```bash
   git push -u origin master
   ```
   
   Wenn gefragt:
   - Username: dein-github-username
   - Password: Gib den Token ein (nicht dein Passwort!)

═══════════════════════════════════════════════════════════════════════════════

## SCHRITT 6: Nach dem Push überprüfen

1. Öffne https://github.com/dein-username/DiskAnalyzer
2. Du solltest sehen:
   ✓ Alle Dateien sind vorhanden
   ✓ Das README.md wird angezeigt
   ✓ Die Folder-Struktur ist korrekt
   ✓ Tags/Releases sind sichtbar

═══════════════════════════════════════════════════════════════════════════════

## REPOSITORY-SETTINGS KONFIGURIEREN (Empfohlen)

Nach dem Push, konfiguriere folgendes:

### 1. Beschreibung
   Settings → Repository details
   Description: "TreeSize Alternative - Hochperformante Festplattenanalyse"
   Website: https://github.com/dein-username/DiskAnalyzer

### 2. Topics
   Klicke "Add topics" und füge hinzu:
   - diskanalyzer
   - delphi
   - windows
   - treesize
   - disk-space-analysis

### 3. Branches schützen
   Settings → Branches
   "Add rule"
   Branch name pattern: main (oder master)
   - ✓ Require pull request reviews
   - ✓ Require status checks to pass

### 4. Issues aktivieren
   Settings → Features
   ✓ Issues
   ✓ Discussions (optional)

### 5. Actions aktivieren
   Falls du GitHub Actions nutzen möchtest:
   Settings → Actions → General
   - Allow all actions

═══════════════════════════════════════════════════════════════════════════════

## LOKALE KOMMANDOS ZUM TESTEN

Bevor du pushst, kannst du lokal testen:

```bash
# Zeige alle Branches
git branch -a

# Zeige Commit-Historie
git log --oneline

# Zeige Status
git status

# Zeige Remotes
git remote -v

# Zeige Tags
git tag -l
```

═══════════════════════════════════════════════════════════════════════════════

## HÄUFIGE FEHLER

### Fehler: "fatal: not a git repository"
Lösung: Stelle sicher, dass du im Projekt-Verzeichnis bist:
```bash
cd /path/to/DiskAnalyzer
```

### Fehler: "remote origin already exists"
Lösung: Entferne die alte Remote und füge neue hinzu:
```bash
git remote remove origin
git remote add origin https://github.com/dein-username/DiskAnalyzer.git
```

### Fehler: "Permission denied (publickey)"
Lösung: SSH-Schlüssel ist nicht konfiguriert. Verwende HTTPS Token oder
richte SSH-Schlüssel korrekt ein.

### Fehler: "Branch 'master' set up to track remote 'origin/master'"
Das ist OK! Das bedeutet, dass dein Branch richtig konfiguriert ist.

═══════════════════════════════════════════════════════════════════════════════

## NÄCHSTE SCHRITTE

Nach erfolgreichem Push:

1. ✅ GitHub README überprüfen (README_GITHUB.md)
2. ✅ Issue-Templates testen
3. ✅ Pull Request Template überprüfen
4. ✅ Actions/Workflows überprüfen
5. ✅ GitHub Pages aktivieren (optional)

## WEITERE TIPPS

- Nutze "Releases" Feature für offizielle Releases
- Aktiviere "Discussions" für Community-Feedback
- Nutze GitHub Projects für Roadmap-Verwaltung
- Füge Branch-Protection Rules hinzu
- Nutze GitHub Actions für CI/CD

═══════════════════════════════════════════════════════════════════════════════

PROJEKT-INFORMATIONEN:

Repository:        /home/claude/DiskAnalyzer/
Aktuelle Version:  v1.0.0
Commits:           1
Tags:              v1.0.0
Dateien:           21
Lizenz:            MIT
Sprache:           Delphi (Pascal)

═══════════════════════════════════════════════════════════════════════════════

Fragen? Überprüfe GitHub Help:
https://docs.github.com/en/get-started/quickstart/create-a-repo

Viel Erfolg beim Hochladen! 🚀
